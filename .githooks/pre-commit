#!/usr/bin/env bash
set -euo pipefail

root="$(git rev-parse --show-toplevel)"
cd "$root"

if ! command -v just >/dev/null 2>&1; then
  echo "pre-commit: just is required (https://github.com/casey/just)" >&2
  exit 1
fi

echo "pre-commit: just lint"
exec just lint
